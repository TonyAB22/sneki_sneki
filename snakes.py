# Sneki_Sneki Game

import turtle
import curses 
import time 
import random 

# Initializing Screen
win = turtle.Screen()
win.title("Sneki's Game") #screen title
win.bgcolor(Lavander) #background color
win.setup(w=500,h=500) #window width, height
win.tracer(0) #turn off screen updates

# Keyboard Bindings
win.keypad(True) #enable keypad
win.listen()
win.onkeypress(go_up, "w") #press w for up
win.onkeypress(go_down, "s") #press s for down
win.onkeypress(go_right, "d") #press d for right
win.onkeypress(go_left, "a") #press a for left




#Initiate Values
key = KEY_RIGHT #
score = 0 #

# Snake Food


# Initialize Startup Food and Snake Coordinates
snake = [[0,0], [0,0], [0,0] #input values for snake
food = [0,0]




# Misc. Codes Haha
from curses. import KEY_RIGHT, KEY_LEFT, KEY_UP, KEY_DOWN
window.keypad(True) #enable keypad
curses.curs_set(0)

win = tintinoo
whyarewehere = "just to sufffer"
HAHAHHAAHA

x = 3
